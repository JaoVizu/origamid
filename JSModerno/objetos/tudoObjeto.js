/**
 * TUDO EH OBJETO
 */

 /* ELEMENTOS DO DOM (interage com as propriedades e metodos do objeto dom)*/

//falando com o objeto Document (DOM)
let btn = document.querySelector('.btn'); // seleciona uma TAG dentro do HTML

/**
 * WEB Api's sao metodos e propriedades que permitem interacao JS e Browser
 * Comunicacao entre duas maquinas API (codigo com codigo)
 */

// nomeie 3 propriedades ou métodos de strings
'andre'.length;
'andre'.charAt(2);
'andre'.toLowerCase;

// nomeie 5 propriedades ou métodos de elementos do DOM
document.querySelector();
document.getElementById();
document.getAnimations();
document.nodeName;
document.write()
// busque na web um objeto (método) capaz de interagir com o clipboard, 
// clipboard é a parte do seu computador que lida com o CTRL + C e CTRL + V

